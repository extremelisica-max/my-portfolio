# Спецификация компонента: [Название]

> Заполняется Product Designer перед передачей в разработку.
> Frontend Developer не начинает работу без заполненной спеки.

---

## Назначение
Что делает компонент. Одно предложение.

## Расположение
Секция страницы, порядок в иерархии.

## Данные
```typescript
// Интерфейс данных (заполняет Strategy Analyst)
interface ComponentProps {
  // ...
}
```

## Визуальная спецификация

### Размеры и отступы (8px grid)
| Элемент | Свойство | Токен Tailwind |
|---------|----------|---------------|
| Wrapper | padding  | `px-6 py-16`  |
| Title   | margin-bottom | `mb-8`   |
| ...     | ...      | ...           |

### Типографика
| Элемент | Размер | Вес | Цвет |
|---------|--------|-----|------|
| Заголовок | `text-2xl` | `font-semibold` | `text-zinc-900` |
| ...     | ...    | ... | ...  |

### Состояния компонента
- `default` — описание
- `hover` — описание
- `focus` — описание (обязательно для интерактивных)
- `disabled` — если применимо

### Адаптивность (mobile-first)
| Брейкпоинт | Поведение |
|------------|-----------|
| `base` (< 640px) | ... |
| `sm` (640px+) | ... |
| `lg` (1024px+) | ... |

## Доступность (WCAG 2.2 AA)
- ARIA-роли и атрибуты
- `data-testid` значения
- `data-ai-action` значения
- Минимальный контраст: 4.5:1 для текста

## Зависимости
Какие ui-компоненты использует (из `components/ui/`).
